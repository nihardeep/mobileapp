import re

with open('style.css', 'r') as f:
    css = f.read()

# Remove the block of CSS
css = re.sub(r'/\* ==========================================================================\n   IOS-STYLE CARD STACK ANIMATION\n   ========================================================================== \*/.*?/\* ==========================================================================\n   INSTA STORIES CAROUSEL\n   ========================================================================== \*/', '/* ==========================================================================\n   INSTA STORIES CAROUSEL\n   ========================================================================== */', css, flags=re.DOTALL)

with open('style.css', 'w') as f:
    f.write(css)

print("CSS reverted")
